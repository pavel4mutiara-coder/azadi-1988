
{
  "name": "azadi-social-welfare",
  "version": "1.0.0",
  "description": "Azadi Social Welfare Organization Android App",
  "main": "index.tsx",
  "scripts": {
    "build": "npx cap copy",
    "sync": "npx cap sync android",
    "open:android": "npx cap open android",
    "make:apk": "npx cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/cli": "^6.0.0"
  }
}
